This project uses PEAR::Console_Table to make the tables.
So, in order to run this code you need to install it first.
It also uses PHPUnit to test the codes.
To run the code just run the index.php file